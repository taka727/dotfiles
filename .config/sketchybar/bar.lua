local colors = require("colors")

sbar.bar({
  height = 28,
  position = "top",
  sticky = true,
  color = colors.bar_bg,
  border_width = 0,
  blur_radius = 0,
  padding_left = 12,
  padding_right = 12,
  corner_radius = 0,
  y_offset = 0,
})
